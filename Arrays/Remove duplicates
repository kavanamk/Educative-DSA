
def removeDuplicates(nums):
    cur = nums[0]
    for num in nums[1:]:
        if num==cur:
            nums.remove(num)
        else:
            cur = num
    return nums
    
    
nums = [1,1,2,2,3,4,4,5]
print(removeDuplicates(nums))
