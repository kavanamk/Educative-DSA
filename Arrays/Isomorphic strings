
def isIsomorphic(s,t):
    d={}
    for ch1,ch2 in zip(s,t):
        if ch1 not in d:
            d[ch1]=ch2
        else:
            if ch2!=d[ch1]:
                return False
    return True
    
    
s = "paper"
t = "title"
print(isIsomorphic(s,t))
